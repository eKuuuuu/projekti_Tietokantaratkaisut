server:
    port: 8081

spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/projekti
    username: api_backend
    password: ApiSalasana2024!
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none